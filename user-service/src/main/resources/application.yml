server:
  port: 8081 # HTTP port (for actuator, etc.)

spring:
  application:
    name: user-service
  cloud:
    eureka:
      client:
        serviceUrl:
          defaultZone: http://localhost:8761/eureka/

grpc:
  server:
    port: 9091 # gRPC server port
#  client:
#    notification-service: # Client configuration for notification-service
#      address: 'static://localhost:9094' # Placeholder, will use Eureka later
#      negotiationType: plaintext # For development
#      enableKeepAlive: true
#      keepAliveTime: 60s
#      keepAliveTimeout: 20s