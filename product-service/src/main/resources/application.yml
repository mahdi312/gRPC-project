server:
  port: 8082

spring:
  application:
    name: product-service
  cloud:
    eureka:
      client:
        serviceUrl:
          defaultZone: http://localhost:8761/eureka/

grpc:
  server:
    port: 9092